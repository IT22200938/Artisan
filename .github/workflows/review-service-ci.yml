name: Review Service CI/CD

on:
  push:
    branches: [main, master]
    paths:
      - 'review-service/**'
      - '.github/workflows/review-service-ci.yml'
  pull_request:
    branches: [main, master]
    paths:
      - 'review-service/**'
      - '.github/workflows/review-service-ci.yml'

env:
  ECR_REPOSITORY: artisan-review-service

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: review-service
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
          cache-dependency-path: review-service/pom.xml
      - name: Build with Maven
        run: mvn clean package -DskipTests -B
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: review-service-jar
          path: review-service/target/review-service-*.jar
          if-no-files-found: error

  sonarcloud:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    defaults:
      run:
        working-directory: review-service
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
          cache-dependency-path: review-service/pom.xml
      - name: SonarCloud Scan
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          mvn clean verify sonar:sonar \
            -Dsonar.projectKey=${{ github.repository_owner }}_artisan-review-service \
            -Dsonar.organization=${{ github.repository_owner }} \
            -DskipTests
        continue-on-error: true

  deploy:
    needs: [build]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: review-service-jar
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION || 'us-east-1' }}
        if: ${{ secrets.AWS_ACCESS_KEY_ID != '' }}
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
        if: ${{ secrets.AWS_ACCESS_KEY_ID != '' }}
      - name: Build and push Docker image
        working-directory: review-service
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
        if: ${{ secrets.AWS_ACCESS_KEY_ID != '' }}
